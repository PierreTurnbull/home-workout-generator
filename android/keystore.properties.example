# Copy to keystore.properties and fill in your release keystore values.
# Do not commit keystore.properties or the .keystore file itself.
storeFile=../home-workout-generator-release.keystore
storePassword=your-store-password
keyAlias=home-workout-generator
keyPassword=your-key-password
